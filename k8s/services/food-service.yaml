apiVersion: apps/v1
kind: Deployment
metadata:
  name: food-service
  namespace: crab
  labels:
    app: food-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: food-service
  template:
    metadata:
      labels:
        app: food-service
        tier: backend
    spec:
      containers:
        - name: food-service
          image: ghcr.io/jasontm17/crab-food-service:latest
          ports:
            - containerPort: 3004
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3004"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crab-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: crab-secrets
                  key: redis-url
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: food-service
  namespace: crab
spec:
  selector:
    app: food-service
  ports:
    - port: 3004
      targetPort: 3004
  type: ClusterIP
