apiVersion: apps/v1
kind: Deployment
metadata: { name: notification-service, namespace: crab, labels: { app: notification-service } }
spec:
  replicas: 2
  selector: { matchLabels: { app: notification-service } }
  template:
    metadata: { labels: { app: notification-service } }
    spec:
      containers:
        - name: notification-service
          image: crab/notification-service:latest
          ports: [{ containerPort: 3007 }]
          envFrom:
            - configMapRef: { name: crab-config }
            - secretRef: { name: crab-secrets }
          env: [{ name: PORT, value: "3007" }]
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: 500m, memory: 512Mi }
          livenessProbe: { httpGet: { path: /api/v1/health, port: 3007 }, initialDelaySeconds: 30 }
          readinessProbe: { httpGet: { path: /api/v1/health, port: 3007 }, initialDelaySeconds: 5 }
---
apiVersion: v1
kind: Service
metadata: { name: notification-service, namespace: crab }
spec:
  selector: { app: notification-service }
  ports: [{ port: 3007, targetPort: 3007 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: notification-service-hpa, namespace: crab }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: notification-service }
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
