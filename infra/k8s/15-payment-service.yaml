apiVersion: apps/v1
kind: Deployment
metadata: { name: payment-service, namespace: crab, labels: { app: payment-service } }
spec:
  replicas: 2
  selector: { matchLabels: { app: payment-service } }
  template:
    metadata: { labels: { app: payment-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: payment-service
          securityContext:
            allowPrivilegeEscalation: false
          image: nguyenson1710/crab-mobile-payment-service:latest
          ports: [{ containerPort: 3005 }]
          envFrom:
            - configMapRef: { name: crab-config }
            - secretRef: { name: crab-secrets }
          env: [{ name: PORT, value: "3005" }]
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: 500m, memory: 512Mi }
          livenessProbe: { httpGet: { path: /api/v1/health, port: 3005 }, initialDelaySeconds: 30 }
          readinessProbe: { httpGet: { path: /api/v1/health, port: 3005 }, initialDelaySeconds: 5 }
---
apiVersion: v1
kind: Service
metadata: { name: payment-service, namespace: crab }
spec:
  selector: { app: payment-service }
  ports: [{ port: 3005, targetPort: 3005 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: payment-service-hpa, namespace: crab }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: payment-service }
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
